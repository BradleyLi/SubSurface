# Requirements for the ml-models project
# Target: Python 3.12, CUDA 13
# Note: cudf and cupy are RAPIDS GPU packages. Pip wheels for Python 3.12 + CUDA 13 may not exist.
# Prefer installing cudf/cupy via conda/mamba if pip install fails (see below).

geopandas>=1.1.3
xgboost
shap

# cudf and cupy placeholders (do NOT rely on plain `pip install cudf`)
# If a matching pip wheel exists, install it explicitly, e.g.:
#   pip install cudf-cu13==<version> cupy-cuda13x==<version>
# Otherwise use conda/mamba (recommended):
#   mamba install -y -c rapidsai -c nvidia -c conda-forge cudf cudatoolkit=13.0 cupy

# CPU-only fallback (if no GPU/cudf):
# pandas
# Optional: add other project-specific requirements here
