# Copy this file to secrets.toml (same folder) and paste your public token (pk.*)
# Streamlit does NOT read this .example file — only secrets.toml works.
# https://account.mapbox.com/access-tokens/
MAPBOX_API_KEY = "pk.your_mapbox_token_here"
