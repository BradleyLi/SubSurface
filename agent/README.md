# Agent

Narrative and assistant-style logic that turns model signals into human-readable insights.

Current modules:
- `agent/why_failing_agent.py` — natural-language explanation for why a pipe is failing
